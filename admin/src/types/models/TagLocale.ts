import { Tag } from './Tag';
import { Locale } from './Locale';

export type TagLocale = {
  tag_id?: number;
  locale?: number;
  name: string;
  created_at?: string;
  updated_at?: string;
  tag?: Tag;
  locale_tag_locale_localeTolocale?: Locale;
}