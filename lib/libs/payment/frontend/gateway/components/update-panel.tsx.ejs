import FormPanel, { FormPanelRef } from "@/components/panels/form-panel";
import { Overlay } from "@/components/custom/overlay";
import { TabPanel } from "@/components/panels/tab-panel";
import { useGatewayGet, useGatewayUpdate } from "@/features/payment/gateway";
import useEffectAfterFirstUpdate from "@/hooks/use-effect-after-first-update";
import { Gateway } from "@/types/models";
import { useState, forwardRef, useImperativeHandle, useRef } from "react";
import { useTranslation } from "react-i18next";
import { IconEdit, IconPlus, IconTrash } from "@tabler/icons-react";

export type GatewayUpdatePanelProps = {
  data: Gateway;
  onUpdated?: (data: Gateway) => void;
};

const GatewayUpdatePanel = forwardRef(
  ({ data, onUpdated }: GatewayUpdatePanelProps, ref) => {
    const { t } = useTranslation(["actions", "fields", "translations"]);
    const { data: item, isLoading } = useGatewayGet(data.id as number);
    const { mutate: gatewayUpdate } = useGatewayUpdate();
    const formRef = useRef<FormPanelRef>(null);

    useEffectAfterFirstUpdate(() => {
      if (item && formRef.current) {
        formRef.current.setValuesFromItem(item);
      }
    }, [item]);

    useImperativeHandle(ref, () => ({}));

    return (
      <TabPanel
        activeTabIndex={0}
        tabs={[
          {
            title: t("details", { ns: "actions" }),
            children: (
              <Overlay loading={isLoading}>
                <FormPanel
                  ref={formRef}
<<<<<<< HEAD:admin/src/pages/cbc/coin/components/coin-update-panel.tsx
                  fields={[]}
=======
                  fields={[
                    {
                      name: "name",
                      label: { text: t("gateway.name", { ns: "fields" }) },
                      type: EnumFieldType.TEXT,
                      required: true,
                    },
                  ]}
>>>>>>> 50c5a03d428dc939c5a678ea0cd821d0075e3bd4:lib/libs/payment/frontend/gateway/components/update-panel.tsx.ejs
                  button={{ text: t("save", { ns: "actions" }) }}
                  onSubmit={(data) => {
                    gatewayUpdate({
                      id: data.id,
                      data,
                    });
                    if (typeof onUpdated === "function") {
                      onUpdated(data);
                    }
                  }}
                />
              </Overlay>
            ),
          },
        ]}
      />
    );
  },
);

GatewayUpdatePanel.displayName = "GatewayUpdatePanel";

export default GatewayUpdatePanel;
